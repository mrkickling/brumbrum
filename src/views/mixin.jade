mixin add_income_modal(id, title, text, group_id)
  div.modal.fade(id=id, tabindex="-1", role="dialog")
    div.modal-dialog(role="document")
      div.modal-content
        div.modal-header
          h3= title
        div.modal-body
          p= text
          form.form-group(id="ajax-form-add-income", action="/group/#{group_id}/income")
            label(for="description") Beskrivning
            input.form-control(type="text", name="description", id="description", placeholder="Beskrivning")
            label(for="cost") Kostnad
            input.form-control(type="text", name="cost", id="cost", placeholder="Kostnad")
            label(for="time") Tidpunkt
            input.form-control(type="date", name="time", id="time", placeholder="Kostnad")
            input.form-control(type="submit" value="Lägg till")

mixin add_expense_modal(id, title, text, group_id)
  - status = status || 'danger'
  - no = no || 'No'
  - yes = yes || 'Yes'
  div.modal.fade(id=id, tabindex="-1", role="dialog")
    div.modal-dialog(role="document")
      div.modal-content
        div.modal-header
          h3= title
        div.modal-body
          p= text
          form.form-group(id="ajax-form-add-expense", action="/group/#{group_id}/expense")
            label(for="description") Beskrivning
            input.form-control(type="text", name="description", id="description", placeholder="Beskrivning")
            label(for="cost") Kostnad
            input.form-control(type="text", name="cost", id="cost", placeholder="Kostnad")
            label(for="time") Tidpunkt
            input.form-control(type="date", name="time", id="time", placeholder="Kostnad")
            input.form-control(type="submit" value="Lägg till")

mixin add_trip_modal(id, title, text, group_id)
  - status = status || 'danger'
  - no = no || 'No'
  - yes = yes || 'Yes'
  div.modal.fade(id=id, tabindex="-1", role="dialog")
    div.modal-dialog(role="document")
      div.modal-content
        div.modal-header
          h3= title
        div.modal-body
          p= text
          form.form-group(id="ajax-form-add-trip", action="/group/#{group_id}/trip")
            label(for="description") Beskrivning
            input.form-control(type="text", name="description", id="description", placeholder="Beskrivning")
            label(for="cost") Kostnad
            input.form-control(type="text", name="cost", id="cost", placeholder="Kostnad")
            label(for="time") Tidpunkt
            input.form-control(type="date", name="time", id="time", placeholder="Kostnad")
            input.form-control(type="submit" value="Lägg till")
